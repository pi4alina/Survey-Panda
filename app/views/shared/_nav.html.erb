<nav<%= " id=#{@body_id}" if @body_id %>>
  <div class="left">
  <%= link_to "Survey Panda", root_path, class: 'home' %>
  </div>
  <div class="right">
    <%if session[:user_id]%>
      <%=button_to 'Log Out', logout_path, method: :delete%>
    <%else%>
      <div class="login"><%=button_to 'Log In', login_path, method: :get%></div>
      <div class="signup"><%=button_to 'Sign Up', signup_path, method: :get%></div>
    <%end%>
  </div>
</nav>